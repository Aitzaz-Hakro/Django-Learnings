# Django App1 — Daily Learning Progress

---

## Friday, June 12, 2026

- Set up **Pipenv** and fixed Windows **PATH** so `pip` and `pipenv` work in the terminal.
- Installed **Django 6.0.6** and created project **firstproject** with app **firstapp**.
- Learned Django structure: `settings`, `urls`, `views`, `models`, `forms`, `templates`, `admin`.
- Built **function-based**, **class-based**, and **form** views.
- Created `Students` and `Admission` models; saved data with **ModelForm** (`form.is_valid()` → `form.save()`).
- Mapped URLs and rendered an admission form in `index.html` with `{% csrf_token %}`.
- Fixed **403 CSRF** errors by adding the token to POST forms.
- Used **Django admin** to view and manage admission records.
- Ran the dev server at `http://127.0.0.1:8000/`; used **SQLite**, prepared **MySQL** config for later.
- Connected to **MySQL 8.0** with `mysql -u root -p`.

---

<!-- Add new daily entries above this line, newest first -->
